# The media register CSVs are generated by tools/media_manifest.py. Their cells
# legitimately contain newlines inside quoted multi-line fields (narration,
# alt text), so git must not normalise their line endings — otherwise a
# Windows checkout would rewrite them and every regeneration would produce a
# spurious diff.
*.csv -text
*.xlsx binary
