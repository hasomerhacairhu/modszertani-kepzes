# The media register CSVs are generated by tools in 02 Tervezet/Média-assetek/_build.
# They legitimately contain CRLF inside quoted multi-line fields, so git must not
# normalise their line endings — otherwise every regeneration produces spurious churn.
*.csv -text
*.xlsx binary
